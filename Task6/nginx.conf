http {
    # Лимит: 10 запросов в минуту на клиента (по IP)
    # rate=10r/m → 10 requests per minute
    limit_req_zone $binary_remote_addr zone=partner_limit:10m rate=10r/m;

    # При превышении лимита — HTTP 429 Too Many Requests
    limit_req_status 429;

    # Настройка upstream для балансировки нагрузки
    upstream backend_servers {
        server backend1.example.com;
        server backend2.example.com;
        server backend3.example.com;
    }

    server {
        listen 80;

        location / {
            # Без burst: строго не более 10 r/m; лишние запросы → 429
            limit_req zone=partner_limit nodelay;

            proxy_pass http://backend_servers;
        }
    }
}
